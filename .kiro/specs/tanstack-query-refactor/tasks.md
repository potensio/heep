# Implementation Plan

- [x] 1. Set up TanStack Query infrastructure
  - [x] 1.1 Install TanStack Query dependencies
    - Install `@tanstack/react-query` package
    - _Requirements: 1.1_
  - [x] 1.2 Create QueryClient configuration
    - Create `src/lib/query-client.ts` with retry, staleTime, and gcTime settings
    - Configure retry count to 3 with exponential backoff
    - Set staleTime to 30 seconds
    - _Requirements: 1.1, 1.2, 1.3_
  - [x] 1.3 Create query keys module
    - Create `src/lib/query-keys.ts` with centralized query key definitions
    - Define notifications query keys (all, list, detail)
    - _Requirements: 2.2_
  - [x] 1.4 Integrate QueryClientProvider in app root
    - Update `app/_layout.tsx` to wrap app with QueryClientProvider
    - _Requirements: 1.4_

- [x] 2. Implement notification hooks
  - [x] 2.1 Create useFirestoreSubscription hook
    - Create `src/hooks/use-firestore-subscription.ts`
    - Implement generic hook that syncs Firestore listener with QueryClient cache
    - Handle subscription cleanup on unmount
    - _Requirements: 3.1, 3.2, 3.3_
  - [ ]\* 2.2 Write property test for cache update on subscription
    - **Property 4: Cache update on subscription**
    - **Validates: Requirements 3.1**
  - [x] 2.3 Create useNotifications hook
    - Create `src/hooks/use-notifications.ts`
    - Use useQuery with notifications query key
    - Integrate useFirestoreSubscription for real-time updates
    - Compute unreadCount from notifications data
    - Return notifications, unreadCount, isLoading, isError, error, refetch
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_
  - [ ]\* 2.4 Write property test for unread count consistency
    - **Property 1: Unread count consistency**
    - **Validates: Requirements 2.5**
  - [x] 2.5 Create useMarkAsRead hook
    - Create `src/hooks/use-mark-as-read.ts`
    - Use useMutation with optimistic update
    - Implement cache rollback on error
    - Update unread count immediately on mutation
    - _Requirements: 4.1, 4.2, 4.3, 4.4_
  - [ ]\* 2.6 Write property test for optimistic update correctness
    - **Property 2: Optimistic update correctness**
    - **Validates: Requirements 4.2, 4.4**
  - [ ]\* 2.7 Write property test for optimistic rollback on failure
    - **Property 3: Optimistic rollback on failure**
    - **Validates: Requirements 4.3**
  - [x] 2.8 Export hooks from index file
    - Create or update `src/hooks/index.ts` to export all notification hooks
    - _Requirements: 6.3_

- [ ] 3. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [x] 4. Update NotificationsScreen to use new hooks
  - [x] 4.1 Refactor NotificationsScreen component
    - Replace useNotifications from context with new hook
    - Use useMarkAsRead hook for marking notifications
    - Update error handling UI with retry button
    - _Requirements: 2.1, 4.1, 5.1, 5.2, 5.3, 5.4_
  - [x] 4.2 Remove or deprecate NotificationContext
    - Remove NotificationContext.tsx or mark as deprecated
    - Update any remaining imports
    - _Requirements: 6.1, 6.2_

- [ ] 5. Final Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
